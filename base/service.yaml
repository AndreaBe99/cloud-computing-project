apiVersion: v1
kind: Service
metadata:
  name: bet-app
  labels:
    app: bet-app
  namespace: mlops
spec:
  type: LoadBalancer
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: bet-app